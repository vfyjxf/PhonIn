<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
  "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
  "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!-- Pragmatic Checkstyle ruleset: import hygiene + a few safe structural checks.
     Formatting (indentation, braces, whitespace, import order) is owned by Spotless
     (google-java-format), so those Checkstyle modules are intentionally NOT enabled to
     avoid the two tools fighting over the same source. -->
<module name="Checker">
  <property name="charset" value="UTF-8"/>
  <property name="severity" value="error"/>
  <property name="fileExtensions" value="java"/>

  <module name="FileTabCharacter">
    <property name="eachLine" value="true"/>
  </module>

  <module name="LineLength">
    <property name="max" value="140"/>
    <property name="ignorePattern" value="^package \S|^import \S"/>
  </module>

  <module name="TreeWalker">
    <!-- import hygiene: the explicit project rule is "always import, never inline-FQN".
         (Import ORDER/spacing is left to Spotless/google-java-format to avoid conflicts.) -->
    <module name="AvoidStarImport"/>
    <module name="RedundantImport"/>
    <module name="UnusedImports"/>

    <module name="AvoidEscapedUnicodeCharacters">
      <property name="allowByTailComment" value="true"/>
      <property name="allowEscapesForControlCharacters" value="true"/>
    </module>
    <module name="EmptyStatement"/>
    <module name="PackageDeclaration"/>
    <module name="ArrayTypeStyle"/>
    <module name="SimplifyBooleanExpression"/>
    <module name="SimplifyBooleanReturn"/>
  </module>
</module>
