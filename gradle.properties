# Gradle build configuration. The build targets Java 8 bytecode via `options.release.set(8)`;
# the daemon may run on any JDK 8-22 that Gradle 8.8 supports (JDK 21 recommended).
org.gradle.caching=true
org.gradle.jvmargs=-Xmx2g
